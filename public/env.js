// Placeholder — in the Docker deployment the web container overwrites this
// file at start with the hub's real config. In local dev it stays empty and
// the app falls back to the VITE_* variables from .env.
window.__SHOPARE_ENV__ = {};
