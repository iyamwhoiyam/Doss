# Supabase project: enova-brain (ref rjvcynsojdgyckhgrlfd)
# The publishable key is safe to expose in a browser bundle ONLY when RLS is
# correctly scoped. See db/migrations/001_revoke_anon_write.sql — the anon role
# currently has write access to core ERP tables and that should be revoked.
VITE_SUPABASE_URL=https://rjvcynsojdgyckhgrlfd.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
